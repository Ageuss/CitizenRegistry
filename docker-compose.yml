version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: citizen_db
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Admin@123!
    ports:
      - "1433:1433"